<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seoulfood.helloworld.dao.DataBookDAO"> <!-- DAO를 직접 연결해준다 -->
    <select id="getBookInfo" resultType="com.seoulfood.helloworld.domain.Book">
        SELECT
            ID,
            NAME,
            WRITER,
            PRICE,
            GENRE,
            PUBLISHER,
            CNT
        FROM BOOKS
    </select>
    <select id="getBookName" parameterType="string" resultType="string">
        SELECT
            NAME
        FROM BOOKS
        WHERE
            ID = #{id}
    </select>
</mapper>